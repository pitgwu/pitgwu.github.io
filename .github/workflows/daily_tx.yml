name: 每日台股盤後資料更新

on:
  schedule:
    - cron: '0 6 * * 1-5'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10' # 修正：升級到 3.10 以支援 yfinance
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install yfinance pandas pytz
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run script
        run: python fetch_txf_5m_daily.py

      - name: Commit and Push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          # 確保路徑正確
          git add stock_train/data_txf_5m_daily/txf_5m_daily.csv || echo "No files to add"
          git commit -m "📈 Auto update TX data: $(date +'%Y-%m-%d')" || exit 0
          git push
